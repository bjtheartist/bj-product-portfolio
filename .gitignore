# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.vercel
.env*.local

# marketing/ is a standalone PRIVATE nested repo (bjtheartist/kivara-gtm) —
# confidential GTM state (prospects, client notes, pricing) that must never be
# tracked in this PUBLIC repo. The deployed business card is public/card.html;
# marketing/business-card.html is the private source copy.
marketing/
